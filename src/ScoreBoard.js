import react ,{useState, useEffect} from "react";

function ScoreBoard () {
    const [scores, setScores] = useState([]);

    return (
        <div>
            <h1>This is scores</h1>
        </div>
    );

}

export default ScoreBoard;